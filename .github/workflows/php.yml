name: Build HydePHP Site

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: hydephp/action@clean-up-code-and-restructure-source-files
        with:
          env: CUSTOM=ENV

